@namespace SuperMarket.Models
@model Category

<div class="container">
    @if (Model != null)
    {
        <h3>Category: @Model.Name</h3>
        <hr>
    }

    @if (Model != null)
    {
        <partial name="_Category" model="@Model" />
    }
</div>